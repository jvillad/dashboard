import AddItem from '@/components/AddItem';
import Layout from '@/components/Layout';

const NewProduct = () => {
  return (
    <Layout>
      <AddItem />
    </Layout>
  );
};

export default NewProduct;
